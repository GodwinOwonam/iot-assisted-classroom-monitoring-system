export enum PROFILE_ENUMS {
  NOT_FOUND = 'Profile does not exist for this user!',
  UPDATE_SUCCESS = 'Profile updated successfully!',
  EMAIL_CONFLICT = 'A store with that email already exists.',
  UNKNOWN_ERROR = 'Could not update profile! Unknown Error!',
}
